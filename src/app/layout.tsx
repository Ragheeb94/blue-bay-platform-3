import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Blue Bay Mobility Inc. | Complex Rehab Technology — Tustin, CA",
    template: "%s | Blue Bay Mobility",
  },
  description:
    "Blue Bay Mobility provides complex rehab technology, power wheelchairs, manual wheelchairs, seating & positioning, and mobility equipment across the U.S. Medicare & insurance specialists.",
  keywords: ["mobility equipment", "power wheelchairs", "complex rehab technology", "CRT", "Tustin California", "Medicare wheelchairs"],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
      <body className="flex flex-col min-h-screen bg-white text-gray-900">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
